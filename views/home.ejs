<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Logo Maker</title>
  <style>
    /* ===== Global Styles ===== */
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }
    body {
      background: #f5f7fa;
      color: #333;
      line-height: 1.6;
    }
    a {
      text-decoration: none;
      color: inherit;
    }

    /* ===== Navbar ===== */
    header {
      background: #222;
      color: #fff;
      padding: 1rem 2rem;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }
    header h1 {
      font-size: 1.5rem;
      letter-spacing: 1px;
    }
    nav a {
      margin: 0 10px;
      color: #fff;
      font-weight: 500;
      transition: color 0.3s;
    }
    nav a:hover {
      color: #ff9800;
    }

    /* ===== Hero Section ===== */
    .hero {
      height: 90vh;
      display: flex;
      justify-content: center;
      align-items: center;
      flex-direction: column;
      text-align: center;
      background: linear-gradient(120deg, #4e54c8, #8f94fb);
      color: #fff;
      padding: 0.5rem;
    }
    .hero h2 {
      font-size: 2.5rem;
      margin-bottom: 1rem;
    }
    .hero p {
      font-size: 1.2rem;
      margin-bottom: 2rem;
    }
    .hero .btn {
      display: inline-block;
      padding: 12px 25px;
      margin: 5px;
      background: #ff9800;
      color: #fff;
      border-radius: 30px;
      transition: 0.3s;
      font-weight: bold;
    }
    .hero .btn:hover {
      background: #e67e22;
      transform: scale(1.05);
    }

    /* ===== Features Section ===== */
    .features {
      padding: 3rem 2rem;
      text-align: center;
      background: #fff;
    }
    .features h3 {
      font-size: 2rem;
      margin-bottom: 1rem;
    }
    .feature-cards {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
      gap: 20px;
      margin-top: 2rem;
      max-width: 1200px;
      margin-left: auto;
      margin-right: auto;
    }
    .card {
      background: #f0f0f5;
      padding: 1.5rem;
      border-radius: 10px;
      transition: 0.3s;
    }
    .card:hover {
      background: #e0e0eb;
      transform: translateY(-5px);
    }
    .card h4 {
      margin-bottom: 10px;
      color: #4e54c8;
    }

    /* ===== Footer ===== */
    footer {
      background: #222;
      color: #fff;
      text-align: center;
      padding: 1rem;
      margin-top: 2rem;
    }
  </style>
</head>
<body>

  <%- include('partials/nav') %>

  <section class="hero">
    <h2>Your Logo, Your Story, Your Style.</h2>
    <p>Easy. Fast. Free to start. </p>
    <p>Customize your logo with icons, colors and text.</p>
    <a href="/editor" class="btn">Create Logo</a>
    <a href="/templates" class="btn">Explore Templates</a>
  </section>

  <section class="features" id="features">
    <h3>Why Choose Logo Maker?</h3>
    <div class="feature-cards">
      <div class="card">
        <h4>🎨 Easy Customization</h4>
        <p>Edit text, colors, fonts, and icons with just a few clicks.</p>
      </div>
      <div class="card">
        <h4>⚡ Fast & Interactive</h4>
        <p>Drag-and-drop editor with live preview for instant results.</p>
      </div>
      <div class="card">
        <h4>📂 Save & Export</h4>
        <p>Download logos in PNG, JPG, or SVG and use them anywhere.</p>
      </div>
    </div>
  </section>

  <footer>
    <p>&copy; 2025 Logo Maker | Built with Node.js & Express</p>
  </footer>

</body>
</html>